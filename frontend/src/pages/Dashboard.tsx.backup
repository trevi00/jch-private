import { useQuery } from '@tanstack/react-query'
import { Briefcase, TrendingUp, Calendar, Award, Target, Clock, MessageSquare, Languages, Zap, Users, FileText, Building2, UserCheck } from 'lucide-react'
import { apiClient } from '@/services/api'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card'
import { Badge } from '@/components/ui/Badge'
import { Button } from '@/components/ui/Button'
import { Link } from 'react-router-dom'
import { useAuthStore } from '@/hooks/useAuthStore'
import { UserType } from '@/types/api'

export default function Dashboard() {
  const { user } = useAuthStore()
  const { data: dashboardData, isLoading } = useQuery({
    queryKey: ['dashboard'],
    queryFn: () => apiClient.getDashboardData(),
  })

  if (isLoading) {
    return (
      <div className="flex items-center justify-center h-64">
        <div className="animate-spin rounded-full h-12 w-12 border-4 border-primary-200 border-t-primary-600 shadow-lg"></div>
      </div>
    )
  }

  // Company user dashboard
  if (user?.userType === UserType.COMPANY) {
    return (
      <div className="space-y-8 p-6">
        <div className="text-center md:text-left">
          <h1 className="text-4xl font-bold text-gray-900 mb-2">기업 대시보드</h1>
          <p className="text-xl text-gray-600">채용 현황과 기업 활동을 한눈에 확인하세요</p>
        </div>

        {/* Company Stats Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-6">
          <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm font-medium text-gray-600 mb-1">등록한 공고</p>
                  <p className="text-3xl font-bold text-gray-900">8</p>
                  <p className="text-xs text-green-600 mt-1">+2 이번 주</p>
                </div>
                <div className="p-3 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl shadow-lg">
                  <FileText className="w-8 h-8 text-white" />
                </div>
              </div>
            </CardContent>
          </Card>

          <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm font-medium text-gray-600 mb-1">지원자 수</p>
                  <p className="text-3xl font-bold text-gray-900">156</p>
                  <p className="text-xs text-orange-600 mt-1">+23 이번 주</p>
                </div>
                <div className="p-3 bg-gradient-to-r from-green-500 to-emerald-600 rounded-xl shadow-lg">
                  <Users className="w-8 h-8 text-white" />
                </div>
              </div>
            </CardContent>
          </Card>

          <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm font-medium text-gray-600 mb-1">면접 예정</p>
                  <p className="text-3xl font-bold text-gray-900">12</p>
                  <p className="text-xs text-blue-600 mt-1">이번 주</p>
                </div>
                <div className="p-3 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-xl shadow-lg">
                  <Calendar className="w-8 h-8 text-white" />
                </div>
              </div>
            </CardContent>
          </Card>

          <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm font-medium text-gray-600 mb-1">채용 완료</p>
                  <p className="text-3xl font-bold text-gray-900">5</p>
                  <p className="text-xs text-purple-600 mt-1">이번 달</p>
                </div>
                <div className="p-3 bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl shadow-lg">
                  <UserCheck className="w-8 h-8 text-white" />
                </div>
              </div>
            </CardContent>
          </Card>
        </div>

        {/* Company Quick Actions */}
        <div className="bg-gradient-to-r from-primary-50 to-blue-50 rounded-xl p-6 border border-primary-100">
          <h2 className="text-xl font-bold text-gray-900 mb-4 flex items-center">
            <Zap className="w-5 h-5 mr-2 text-primary-600" />
            빠른 실행
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <Link to="/jobs/create" aria-label="채용공고 작성" className="group">
              <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
                <div className="p-3 bg-blue-100 rounded-lg mr-4">
                  <FileText className="w-6 h-6 text-blue-600" />
                </div>
                <div>
                  <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">채용공고 작성</h3>
                  <p className="text-sm text-gray-600">새로운 채용공고 등록</p>
                </div>
              </div>
            </Link>
            
            <Link to="/applications" aria-label="지원자 관리" className="group">
              <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
                <div className="p-3 bg-green-100 rounded-lg mr-4">
                  <Users className="w-6 h-6 text-green-600" />
                </div>
                <div>
                  <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">지원자 관리</h3>
                  <p className="text-sm text-gray-600">지원자 현황 확인</p>
                </div>
              </div>
            </Link>
            
            <Link to="/company/profile" aria-label="기업 정보" className="group">
              <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
                <div className="p-3 bg-purple-100 rounded-lg mr-4">
                  <Building2 className="w-6 h-6 text-purple-600" />
                </div>
                <div>
                  <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">기업 정보</h3>
                  <p className="text-sm text-gray-600">기업 프로필 관리</p>
                </div>
              </div>
            </Link>
          </div>
        </div>

        {/* Company Recent Activity */}
        <div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
          <Card className="shadow-lg border-0" hover="glow">
            <CardHeader className="pb-4">
              <div className="flex items-center justify-between">
                <CardTitle className="text-xl font-bold flex items-center">
                  <Clock className="w-5 h-5 mr-2 text-primary-600" />
                  최근 지원 현황
                </CardTitle>
                <Button variant="ghost" size="sm">
                  전체 보기
                </Button>
              </div>
            </CardHeader>
            <CardContent className="space-y-4">
              {[
                { position: '백엔드 개발자', applicants: '12명', status: '진행 중', statusColor: 'info', time: '1일 전' },
                { position: '프론트엔드 개발자', applicants: '8명', status: '마감', statusColor: 'warning', time: '3일 전' },
                { position: 'DevOps 엔지니어', applicants: '5명', status: '채용 완료', statusColor: 'success', time: '1주 전' }
              ].map((item, index) => (
                <div key={index} className="flex items-center justify-between p-4 bg-gradient-to-r from-gray-50 to-blue-50 rounded-xl hover:shadow-md transition-all duration-300">
                  <div className="flex-1">
                    <p className="font-semibold text-gray-900 mb-1">{item.position}</p>
                    <p className="text-sm text-gray-600 font-medium">{item.applicants} 지원</p>
                  </div>
                  <div className="text-right">
                    <Badge variant={item.statusColor as any} size="sm" className="mb-1">
                      {item.status}
                    </Badge>
                    <p className="text-xs text-gray-500">{item.time}</p>
                  </div>
                </div>
              ))}
            </CardContent>
          </Card>

          <Card className="shadow-lg border-0" hover="glow">
            <CardHeader className="pb-4">
              <div className="flex items-center justify-between">
                <CardTitle className="text-xl font-bold flex items-center">
                  <TrendingUp className="w-5 h-5 mr-2 text-primary-600" />
                  인기 공고
                </CardTitle>
                <Link to="/jobs">
                  <Button variant="ghost" size="sm">
                    더 보기
                  </Button>
                </Link>
              </div>
            </CardHeader>
            <CardContent className="space-y-4">
              {[
                { position: '시니어 Java 개발자', views: '234회', applications: '45명', isHot: true },
                { position: 'React 개발자', views: '189회', applications: '32명', isHot: true },
                { position: 'AI/ML 엔지니어', views: '156회', applications: '28명', isHot: false }
              ].map((item, index) => (
                <div key={index} className="p-4 border border-gray-200 rounded-xl hover:shadow-md hover:border-primary-300 cursor-pointer transition-all duration-300 group">
                  <div className="flex justify-between items-start mb-3">
                    <h4 className="font-semibold text-gray-900 group-hover:text-primary-600 transition-colors">{item.position}</h4>
                    {item.isHot && (
                      <Badge variant="destructive" size="sm">
                        인기
                      </Badge>
                    )}
                  </div>
                  <div className="flex items-center gap-4 text-xs text-gray-500">
                    <span>조회 {item.views}</span>
                    <span>•</span>
                    <span>지원 {item.applications}</span>
                  </div>
                </div>
              ))}
            </CardContent>
          </Card>
        </div>
      </div>
    )
  }

  // General user dashboard (default)
  return (
    <div className="space-y-8 p-6">
      <div className="text-center md:text-left">
        <h1 className="text-4xl font-bold text-gray-900 mb-2">대시보드</h1>
        <p className="text-xl text-gray-600">취업 현황과 활동을 한눈에 확인하세요</p>
      </div>

      {/* Stats Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-6">
        <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
          <CardContent className="p-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-600 mb-1">지원한 공고</p>
                <p className="text-3xl font-bold text-gray-900">12</p>
                <p className="text-xs text-green-600 mt-1">+2 이번 주</p>
              </div>
              <div className="p-3 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl shadow-lg">
                <Briefcase className="w-8 h-8 text-white" />
              </div>
            </div>
          </CardContent>
        </Card>

        <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
          <CardContent className="p-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-600 mb-1">면접 일정</p>
                <p className="text-3xl font-bold text-gray-900">3</p>
                <p className="text-xs text-orange-600 mt-1">이번 주 예정</p>
              </div>
              <div className="p-3 bg-gradient-to-r from-green-500 to-emerald-600 rounded-xl shadow-lg">
                <Calendar className="w-8 h-8 text-white" />
              </div>
            </div>
          </CardContent>
        </Card>

        <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
          <CardContent className="p-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-600 mb-1">취업 점수</p>
                <p className="text-3xl font-bold text-gray-900">85</p>
                <p className="text-xs text-blue-600 mt-1">상위 15%</p>
              </div>
              <div className="p-3 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-xl shadow-lg">
                <Award className="w-8 h-8 text-white" />
              </div>
            </div>
          </CardContent>
        </Card>

        <Card className="hover:shadow-lg transition-all duration-300 border-0 shadow-md" hover="lift">
          <CardContent className="p-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-600 mb-1">스킬 매치</p>
                <p className="text-3xl font-bold text-gray-900">92%</p>
                <p className="text-xs text-purple-600 mt-1">매우 높음</p>
              </div>
              <div className="p-3 bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl shadow-lg">
                <Target className="w-8 h-8 text-white" />
              </div>
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Quick Actions */}
      <div className="bg-gradient-to-r from-primary-50 to-blue-50 rounded-xl p-6 border border-primary-100">
        <h2 className="text-xl font-bold text-gray-900 mb-4 flex items-center">
          <Zap className="w-5 h-5 mr-2 text-primary-600" />
          빠른 실행
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <Link to="/jobs" aria-label="채용정보" className="group">
            <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
              <div className="p-3 bg-blue-100 rounded-lg mr-4">
                <Briefcase className="w-6 h-6 text-blue-600" />
              </div>
              <div>
                <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">채용정보</h3>
                <p className="text-sm text-gray-600">새로운 채용공고 확인</p>
              </div>
            </div>
          </Link>
          
          <Link to="/ai/interview" aria-label="AI 면접" className="group">
            <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
              <div className="p-3 bg-green-100 rounded-lg mr-4">
                <MessageSquare className="w-6 h-6 text-green-600" />
              </div>
              <div>
                <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">AI 면접</h3>
                <p className="text-sm text-gray-600">면접 연습하기</p>
              </div>
            </div>
          </Link>
          
          <Link to="/ai/translation" aria-label="AI 번역" className="group">
            <div className="flex items-center p-4 bg-white rounded-lg shadow-sm hover:shadow-md transition-all duration-300 group-hover:scale-105">
              <div className="p-3 bg-purple-100 rounded-lg mr-4">
                <Languages className="w-6 h-6 text-purple-600" />
              </div>
              <div>
                <h3 className="font-semibold text-gray-900 group-hover:text-primary-600">AI 번역</h3>
                <p className="text-sm text-gray-600">문서 번역하기</p>
              </div>
            </div>
          </Link>
        </div>
      </div>

      {/* Recent Activity */}
      <div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
        <Card className="shadow-lg border-0" hover="glow">
          <CardHeader className="pb-4">
            <div className="flex items-center justify-between">
              <CardTitle className="text-xl font-bold flex items-center">
                <Clock className="w-5 h-5 mr-2 text-primary-600" />
                최근 지원 현황
              </CardTitle>
              <Button variant="ghost" size="sm">
                전체 보기
              </Button>
            </div>
          </CardHeader>
          <CardContent className="space-y-4">
            {[
              { company: '네이버', position: '프론트엔드 개발자', status: '서류 통과', statusColor: 'success', time: '2일 전' },
              { company: '카카오', position: '풀스택 개발자', status: '면접 대기', statusColor: 'warning', time: '1주 전' },
              { company: '라인', position: 'React 개발자', status: '최종 합격', statusColor: 'success', time: '3일 전' }
            ].map((item, index) => (
              <div key={index} className="flex items-center justify-between p-4 bg-gradient-to-r from-gray-50 to-blue-50 rounded-xl hover:shadow-md transition-all duration-300">
                <div className="flex-1">
                  <p className="font-semibold text-gray-900 mb-1">{item.position}</p>
                  <p className="text-sm text-gray-600 font-medium">{item.company}</p>
                </div>
                <div className="text-right">
                  <Badge variant={item.statusColor as any} size="sm" className="mb-1">
                    {item.status}
                  </Badge>
                  <p className="text-xs text-gray-500">{item.time}</p>
                </div>
              </div>
            ))}
          </CardContent>
        </Card>

        <Card className="shadow-lg border-0" hover="glow">
          <CardHeader className="pb-4">
            <div className="flex items-center justify-between">
              <CardTitle className="text-xl font-bold flex items-center">
                <TrendingUp className="w-5 h-5 mr-2 text-primary-600" />
                추천 채용공고
              </CardTitle>
              <Link to="/jobs">
                <Button variant="ghost" size="sm">
                  더 보기
                </Button>
              </Link>
            </div>
          </CardHeader>
          <CardContent className="space-y-4">
            {[
              { company: '카카오', position: '백엔드 개발자', experience: '경력 1-3년', location: '서울 판교', isNew: true },
              { company: '토스', position: 'Node.js 개발자', experience: '경력 2-4년', location: '서울 강남', isNew: true },
              { company: '쿠팡', position: 'Java 개발자', experience: '경력 1-5년', location: '서울 송파', isNew: false }
            ].map((item, index) => (
              <div key={index} className="p-4 border border-gray-200 rounded-xl hover:shadow-md hover:border-primary-300 cursor-pointer transition-all duration-300 group">
                <div className="flex justify-between items-start mb-3">
                  <h4 className="font-semibold text-gray-900 group-hover:text-primary-600 transition-colors">{item.position}</h4>
                  {item.isNew && (
                    <Badge variant="info" size="sm">
                      신규
                    </Badge>
                  )}
                </div>
                <p className="text-sm font-medium text-gray-700 mb-2">{item.company}</p>
                <div className="flex items-center gap-2 text-xs text-gray-500">
                  <span>{item.experience}</span>
                  <span>•</span>
                  <span>{item.location}</span>
                </div>
              </div>
            ))}
          </CardContent>
        </Card>
      </div>
    </div>
  )
}